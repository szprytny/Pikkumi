<%= form_with(model: profile_type, local: true, class: 'form-vertical') do |form| %>
  <% if profile_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(profile_type.errors.count, "error") %> prohibited this profile_type from being saved:</h2>

      <ul>
      <% profile_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="container">
  <div class="form-group">
    <%= form.label :typeName, :class => 'col-xs-2' %>
    <%= form.text_field :typeName, id: :profile_type_typeName %>
  </div>

  <div class="form-group">
    <%= form.submit :class => 'btn btn-primary' %>
  </div>
</div>
<% end %>
